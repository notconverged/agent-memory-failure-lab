# Basic Memory

1. 创建并验证 `amlab-basic-memory`。
2. 为本 run 创建独立 notes 目录和 project 名 `amlab-<run-id>`。
3. 在 18080 端口启动 streamable-http MCP，并临时连接 Codex App。
4. 逐阶段检查 Markdown source of truth、索引、召回与 CRUD。
